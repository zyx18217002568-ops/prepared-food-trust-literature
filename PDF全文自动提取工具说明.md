# PDF全文自动提取工具说明

本仓库已经启用 PDF → UTF-8 全文文本的自动抽取流程，目的是让仓库中的 CNKI / 外文 PDF 能被后续文献索引与论文写作直接检索和通读。

## 组成

- `tools/extract_pdf_text.py`：核心抽取脚本。
- `.github/workflows/extract-pdf-text.yml`：GitHub Actions 自动任务。
- `_全文文本/`：自动生成的全文文本镜像目录。
- `_全文文本/提取状态.md`：每篇 PDF 的抽取质量状态。
- `_全文文本/提取状态.csv`：同一状态表的 CSV 版本。

## 工作方式

1. 将 PDF 上传到仓库任意现有文献目录。
2. GitHub Actions 检测到 `**/*.pdf` 变化后自动运行。
3. Ubuntu runner 安装 Poppler，并执行：

   `python3 tools/extract_pdf_text.py --repo . --clean`

4. 每篇 PDF 在 `_全文文本/` 下生成同路径的 `.pdf.txt` 文件。
5. 文本中保留 `===== PAGE N =====` 页码标记，方便后续回到原 PDF 定位。
6. 自动生成抽取状态表，并把新文本提交回 `master`。

## 状态含义

- `ok`：已抽取足量正文，可用于全文检索和通读。
- `low_text`：正文极少，通常是扫描版、图片 PDF 或文字层损坏，需要另行 OCR。
- `encoding_warning`：替换字符较多，可能存在编码异常，需要人工检查。
- `extract_error` / `exception`：抽取失败，需要单独处理。

## 当前实测

2026-08-28 已在仓库真实文献上完成自动测试：

- 共发现并处理 40 个 PDF；
- 40 个全部判定为 `ok`；
- P0-1 的 8 篇中文核心论文均成功提取；
- 中文正文、英文正文、混合中英文论文均可读；
- 例如《食品召回策略对信任和购买意愿的影响》已成功抽取 10 页正文，并可直接读取摘要、方法、结果和参考文献。

GitHub Actions 的测试运行 `Extract PDF full text` 已成功完成，Checkout、Poppler 安装、全文抽取、状态输出、提交步骤均通过。

## 后续使用原则

后续文献工作采用：

**索引表 → `_全文文本` 定位/通读 → 必要时回 PDF 页码核验 → 正式引用**。

索引只负责导航；涉及样本量、实验处理、模型系数、显著性、作者原意和原句时，应读取对应全文文本并按页码回原 PDF 复核。

## 限制

当前流程不自动运行 OCR。若以后上传真正的扫描版 CNKI PDF，状态表会把它标成 `low_text`，再针对该文件单独增加 OCR 分支即可。现阶段仓库中的 40 个 PDF 均不需要 OCR。
